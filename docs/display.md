# Display and integrity metadata

`public_metadata()` checks the label template SHA-256 before displaying it.
The public route identifier is a display/routing label, not account authentication.
The accent RGB triple supplies the CSS color; it is unrelated to webhook signing.
The Ed25519 JWK has only public verification material and the verify operation.
